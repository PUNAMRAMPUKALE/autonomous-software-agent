def main():

    planner = PlannerAgent()

    story = planner.choose_next_story()

    if story:

        print()

        print("Planner finished.")

        planner.print_selected_story(
            story
        )

        # Perform analysis and generate an implementation plan
        print()
        planner.analyze_and_plan(story)